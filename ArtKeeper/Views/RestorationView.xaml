<UserControl x:Class="ArtKeeper.Views.RestorationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Реставрационные задания" FontSize="24" FontWeight="Bold" Margin="0,0,0,15" Foreground="#333333"/>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Tasks}" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Экспонат" Binding="{Binding Exhibit.Title}" Width="200"/>
                <DataGridTextColumn Header="Мастерская" Binding="{Binding Workshop.WorkshopName}" Width="200"/>
                <DataGridTextColumn Header="Мастер" Binding="{Binding Restorer.FullName}" Width="150"/>
                <DataGridTextColumn Header="Статус" Binding="{Binding Status}" Width="150"/>
                <DataGridTextColumn Header="Дата начала" Binding="{Binding StartDate, StringFormat=dd.MM.yyyy}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>