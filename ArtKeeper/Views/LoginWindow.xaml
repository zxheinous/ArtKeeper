<Window x:Class="ArtKeeper.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:ArtKeeper.ViewModels"
        Title="АртХранитель - Авторизация" Height="450" Width="350" WindowStartupLocation="CenterScreen"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent" MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <Window.DataContext>
        <vm:LoginViewModel/>
    </Window.DataContext>

    <Border Background="White" CornerRadius="12" Margin="15">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" Color="Black" Opacity="0.15" ShadowDepth="5"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Button Content="✕" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="10" 
                    Background="Transparent" BorderThickness="0" FontSize="16" Foreground="#999" 
                    Click="CloseButton_Click" Cursor="Hand"/>

            <StackPanel Grid.Row="1" Margin="30,10,30,30">
                <TextBlock Text="🏛" FontSize="45" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                <TextBlock Text="АртХранитель" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Foreground="#2D2D30"/>
                <TextBlock Text="Система управления фондами" FontSize="13" HorizontalAlignment="Center" Foreground="#888888" Margin="0,0,0,30"/>

                <TextBlock Text="Логин" Foreground="#555555" FontWeight="SemiBold" Margin="0,0,0,5"/>
                <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" 
                         Padding="10,8" FontSize="14" Background="#F5F6F8" BorderBrush="#E0E0E0" BorderThickness="1" Margin="0,0,0,15"/>

                <TextBlock Text="Пароль" Foreground="#555555" FontWeight="SemiBold" Margin="0,0,0,5"/>
                <PasswordBox x:Name="txtPassword" Padding="10,8" FontSize="14" Background="#F5F6F8" BorderBrush="#E0E0E0" BorderThickness="1" Margin="0,0,0,25"/>

                <Button Content="Войти в систему" Command="{Binding LoginCommand}" CommandParameter="{Binding ElementName=txtPassword}" 
                        Background="#007ACC" Foreground="White" FontSize="15" FontWeight="Bold" Padding="10" BorderThickness="0" Cursor="Hand">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="6"/>
                        </Style>
                    </Button.Resources>
                </Button>

                <TextBlock Text="{Binding ErrorMessage}" Foreground="#D32F2F" HorizontalAlignment="Center" Margin="0,15,0,0" TextWrapping="Wrap" TextAlignment="Center"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>